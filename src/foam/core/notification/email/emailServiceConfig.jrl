p({
  class: 'foam.core.notification.email.EmailServiceConfig',
  id: 'smtp'
})
// Microsoft Graph API
p({
  class: 'foam.core.notification.email.EmailServiceConfig',
  id: 'msGraph',
  rateLimit: 4
})
p({
  class: 'foam.core.notification.email.EmailServiceConfig',
  id: 'imap',
  protocol: 'imaps',
  port: '993'
})
